<x-layouts.app title="Produk - CekDulu"><div class="page-header"><div style="max-width:1200px;margin:0 auto"><div style="font-size:12px;color:var(--color-text-tertiary)">Beranda › <span style="color:var(--primary)">Produk</span></div><h1 style="font-size:22px;font-weight:700;margin-top:4px">Semua Produk</h1></div></div><div class="container grid-layout"><aside class="sidebar"><div class="filter-title">Filter</div><div class="filter-group"><h4>Kategori</h4>@foreach($categories as $category)<a class="filter-item" href="{{ route('products.index',array_merge(request()->except('page'),['category'=>$category->slug])) }}"><span>{{ $category->name }}</span></a>@endforeach</div><div class="filter-group"><h4>Brand</h4>@foreach($brands as $brand)<a class="filter-item" href="{{ route('products.index',array_merge(request()->except('page'),['brand'=>$brand->slug])) }}"><span>{{ $brand->name }}</span></a>@endforeach</div></aside><section><div class="listing-topbar"><form action="{{ route('products.index') }}" method="GET" class="listing-search"><input name="search" placeholder="Cari produk..." value="{{ request('search') }}"></form><form method="GET"><select name="sort" class="sort-select" onchange="this.form.submit()"><option value="worth">Worth It Score</option><option value="price">Harga Termurah</option><option value="latest">Terbaru</option></select></form></div><div style="font-size:12px;color:var(--color-text-tertiary);margin-bottom:12px">Menampilkan {{ $products->total() }} produk</div><div class="product-grid">@foreach($products as $product)<x-product-card :product="$product"/>@endforeach</div><div style="margin-top:1.5rem">{{ $products->links() }}</div></section></div></x-layouts.app>
