@props(['product']) @php $best=$product->bestPrice(); @endphp <a href="{{ route('products.show',$product) }}" class="card"><div class="card-img">@if($product->thumbnail)<img src="{{ $product->thumbnail }}" alt="{{ $product->name }}">@else<div class="placeholder-icon">🛍️</div>@endif</div><div class="card-body"><div class="card-name">{{ $product->name }}</div><div class="card-price">Rp {{ number_format($best?->price ?? $product->lowest_price ?? 0,0,',','.') }}</div>@if($best?->original_price)<div class="card-orig">Rp {{ number_format($best->original_price,0,',','.') }}</div>@endif<div class="card-meta"><div class="card-rating">⭐ {{ number_format($best?->rating ?? 0,1) }}</div><div class="worth-badge">{{ $product->worth_it_score }}/100</div></div><div class="mkt-badge">Termurah di <span>{{ $best?->marketplace?->name ?? 'Marketplace' }}</span></div></div></a>
