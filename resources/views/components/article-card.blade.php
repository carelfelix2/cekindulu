@props(['article'])<a href="{{ route('articles.show',$article) }}" class="article-card"><div class="article-img">@if($article->featured_image)<img src="{{ $article->featured_image }}" alt="{{ $article->title }}">@else<div class="placeholder-icon">📝</div>@endif</div><div class="article-body"><span class="article-tag">Rekomendasi</span><div class="card-name">{{ $article->title }}</div><div class="footer-desc">{{ $article->published_at?->format('d M Y') }}</div></div></a>
